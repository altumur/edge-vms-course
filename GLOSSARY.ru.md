# Глоссарий

Термины, которые курс употребляет точно, и аббревиатуры, которые иначе остались бы без расшифровки. Там, где у слова есть бытовое значение и более узкое здешнее, уроки имеют в виду узкое.

## Три слова, которые курс держит раздельно

| Термин | Значение здесь |
|---|---|
| ~~**recorder**~~ | *Устарел (12 сентября 2026).* Рекордер из уроков 5–9 М9: процесс со своей базой, камерами и индексом архива, отличный от коробки, на которой он работал. В форме М10 на сервере лежат хранилища платформы, *ресурс* на его дисках и размещённые там *воркеры* — а их сумма и есть *сервер*, так что пятое слово ушло. Встречается в М9 (история) и в сохранённых ранних редакциях [`where-the-database-lives.md`](./М12_DomainVMS/where-the-database-lives.md) из М12. |
| **Событие** (*Event*) | Наблюдение — детект, тишина, отметка оператора, — которое воркер, держащий эпоху единицы, пишет в бакет этой единицы на ресурсе своего сервера: `<subsystem>/<unit>/e<epoch>/<start>Z.events.jsonl`, идёт запись или нет. Часть платформы, которой пользуется любая подсистема; бакеты VMS лежат рядом с её видео. Закрываются в манифест, отсекаются путём, хранятся своим числом дней. Событий не пишет ни один контроллер; каждый процесс ресурса держит над своими `EventDatabase` — кэш, — а консоль их сливает (М10, уроки 3 и 9; М11, урок 3). |
| **Воркер** (*Worker*) | Шард DriverPack с N назначенными камерами — держит конвейер и маршрутизирует его; устойчивая личность, переносим между серверами, не хранит состояния, кроме spool. То, чем становится рекордер М9 по решению *2c* из М11. 1+ на кластер, по нагрузке. |
| **Ресурс** (*Resource*, работа платформы) | Один на сервер, прибит к нему: задание `resource` платформы (`w2cplatform/resource.py`) бьётся heartbeat'ом о том, что лежит на дисках сервера, под `platform/resources/<server>`, раздаёт бакеты всех подсистем, зеркалит закрытые на следующий ресурс, когда включён `platform/mirror`, возвращает их после замены диска, хранит по строкам `<sub>/retention` каждой подсистемы и гоняет проходы, которые подсистемы на нём регистрируют (у VMS: манифесты и медиа). |
| **Ресурс** (*Resource*) | То, что привязано к серверу: архив на его дисках, GPU для детекторов, сетевая карта в камерном VLAN. Задание Nomad типа `system`, прибитое тем, что у сервера есть; N на кластер; когда оно умирает, ничего не переезжает — потому что переехать не может. |
| **Контроллер** (*Controller*, кластерный) | Единственное задание на кластер, которое размещает камеры по воркерам и перебалансирует по запросу. Не хранит состояния — вычисление над переменными и объектами, — корректен через CAS, безопасен при двух и никогда не на пути восстановления: пока он лежит, ничего из уже работающего не останавливается. Не путать с устаревшим *контроллером домена*, который состояние держал. С урока 6 М10 это `SpecController` платформы, запущенный из `<sub>.subsystem.yaml` подсистемы, — один класс на все подсистемы; `count = 1` — экономия, корректность — на CAS. |
| **Сервер** (*Server*) | Коробка с процессорами и дисками — и, в форме М10, всё, что на ней: реплика хранилищ кластера, задание *resource* платформы на её дисках и те *воркеры*, которые туда поставит планировщик. Nomad-*client*. М10 — это ServerVMS: форма платформы на одном сервере. |
| **Кластер домена** (*Domain cluster*) | Единственный кластер, названный оператором, на котором работают службы домена — подписывающий, размещение, читающее представление, сервер обновлений, удалённый наблюдатель. Сервер внутри него выбирает Nomad. Его raft держит ключ подписывающего и записи локальных пользователей; его хранилище объектов — их резервную копию и объект личности. Если он умирает, службы домена умирают вместе с ним — ограниченно, и запись не умирает. |
| **Рекордер** (*Recorder*) | Воркер подсистемы `rec` (М10, урок 5), единственный, кого размещают поверх архива: его единица работы — *запись* одной камеры (`rec/recordings/<cam>`, явная строка — *Record* на странице), его ёмкость — то, что вмещают диски его сервера. Камеру он не держит: он подписывается на тройник воркера — разделяемая память (`live_shm`), когда они на одном сервере, RTSP-раздача (`live_url`) иначе — и пишет `rec/<cam>/e<epoch>/` на ресурсе своего сервера под своей эпохой; `requires: resource`, `servers: distinct` по умолчанию и `home` на его строке — сервер, чей архив её держит. Сам он не следует ни за кем: он единственный здесь приколочен к дискам, поэтому за ним идёт камера (`near: rec`), а не наоборот. |
| **Шлюз живого видео** (*Live gateway*) | Воркер подсистемы `live` (М10, урок 8): его единица работы — раздача одной камеры, его ёмкость — зрители. Он подписывается на RTP-тройник воркера один раз на камеру и раздаёт её N браузерам по WebRTC (сигналинг WHEP через консоль); единицу раздачи создаёт первый зритель, а удаляет шлюз после льготного периода. `count = N` по запасу на зрителей; размещается по ограничению (публичный адрес, GPU для транскодирования), никогда по имени хоста. Единственный зритель воркера, вместе с консолью; воркер никогда не обслуживает браузер. М12 добавляет TURN, проверку токена и размещение между кластерами (*Кто обслуживает браузеры*). |
| **Консоль** (*Console*, кластерная) | Задание уровня кластера, которое раздаёт UI: статические файлы, фасад API, читающую модель, собранную из heartbeat'ов воркеров и снимка контроллера, TLS, проверку токенов. Не хранит состояния; на домене проксирует правки в консоль владеющего кластера, и решают её гранты. Кластер домена запускает ту же консоль, нацеленную на все кластеры. В отличие от консоли рекордера в М9 (М9, урок 9), которая обслуживает только эти два задания. С урока 7 М10 это собственное задание — по одному на каждом сервере, ничего перед ними — со своим токеном — строки оператора, никогда размещение — и, как контроллер, это `SpecConsole` платформы, запущенный из YAML подсистемы; подсистема добавляет только те маршруты, которые её собственные (у VMS: таймлайн и байты). |
| **Подписывающий домена** (*Domain signer*) | Единственное задание Nomad на домене, которое держит ключи и подписывает — сертификаты для машин, токены для людей. Единственная служба домена, которую нельзя развернуть заново из ничего, и причина, по которой делегированный корень из М13 имеет значение. Хостится одним назначенным кластером. |
| ~~**Domain controller**~~ | **Устарел.** Называл компонент, который дизайн растворил: его база стала переменными и хранилищами объектов кластера, его планировщик стал Nomad, его API переехал на консоль каждого кластера, его точка восстановления растворилась в raft кластера. На домене остались подписывающий, не хранящая состояния функция размещения и читающее представление — ни одно из них не авторитетно, поэтому старое имя вводит в заблуждение. |
| **Площадка** (*Site*) | Там, где камеры физически находятся, — здание, магазин. Единственное из всего этого, что оператор называет сам. **Площадки и кластеры связаны «многие ко многим» намеренно:** кампус — это один домен с тремя кластерами и тремя площадками; облачная установка — один домен с одним кластером, обслуживающим пятьдесят площадок. |

Ещё два, которые легко спутать:

| Термин | Значение здесь |
|---|---|
| **Кластер** (*Cluster*) | Серверы, стоящие достаточно близко, чтобы разделять **сеть, на которую вы поставите запись**, — один LAN, обычно одна серверная. То, что строит М11, и граница, заданная **физикой**. Воркер переезжает при отказе *внутри* кластера и **никогда через его границу** — потому что его видео лежит на дисках этого кластера. |
| **Домен** (*Domain*) | **Кластеры** под одним каталогом, одним CA и одним набором операторов. То, что строит М12, и граница, заданная **администрированием**, а не физикой, — одна установка у одного заказчика, будь в ней один кластер или несколько. Кластер отвечает на вопрос *где это может работать*; домен — на вопрос *что должно работать и в каком кластере*. **Потеря кластера останавливает камеры этого кластера; потеря домена не останавливает ничего из того, что уже пишется.** Дальше домена федерируют, а не строят домен побольше. |
| **Федерация** (*Federation*) | Теперь означает одно: регионы Nomad, сшитые gossip'ом и не разделяющие состояния, — именно так М12 собирает домен из нескольких кластеров. *Другая* федерация — независимые домены под общим корнем доверия — ушла вместе со слоем над доменом: у каждого заказчика один домен, и общего корня нет по замыслу. |
| **Вендор** (*Vendor*) | Дальняя сторона единственной границы продукта, которая не является сетью. Один заказчик — один домен, а домен — верх продукта, так что над ним не слой, а отдельная организация. Вендор может поручиться за железо, выдать право на использование, публиковать обновления и сдавать ёмкость в аренду; он никогда не должен держать корень заказчика, в одиночку вводить коробку в домен или останавливать запись, что-либо придерживая. М14. |
| **Лицензия** (*Licence*, право на использование) | Подписанный вендором документ, называющий **id домена** — никогда не сервер и не коробку — с числом камер, функциями, окном действия и льготным периодом. Тянется через сервер обновлений домена как бандл, лежит в переменных кластера домена, проверяется доменом офлайн по ключу, поставляемому в продукте. Проверяется **на приёме** (размещение отказывает новой камере) и никогда в рантайме: истёкшая лицензия отбирает право расти, а не запись. Отзыва нет — только сроки жизни, а у бессрочной лицензии их нет вовсе. М14, урок 3; потребляющая сторона — М12, урок 5. |
| ~~**Orchestration** (слой)~~ | **Устарело.** В ранних черновиках над доменами был слой, поставлявший ёмкость и доверие. Домен сам разворачивает свои кластеры и сам себе корень; от слоя остался вендор. Nomad по-прежнему *оркестрирует* — внутри кластера, размещая аллокейшены на серверах, — и это значение слова живёт. |
| **Устройство / облако / смешанное** (*Edge / cloud / mixed*) | Где физически стоят серверы домена — на площадке, в арендованном облачном регионе или и там и там в одной системе. Решение о размещении для каждой площадки, а не три продукта: воркер не может определить, на чём из этого он работает. |

---

## Состояние и корректность

| | |
|---|---|
| **Желаемое состояние** (*Desired state*) | Что попросил оператор. Персистентно. |
| **Фактическое состояние** (*Actual state*) | Что работает прямо сейчас. Наблюдается, никогда не сохраняется: всё, что система помнит о фактическом состоянии между перезапусками, — баг. |
| **Сверка** (*Reconciliation*) | Цикл, который закрывает разрыв между этими двумя. Jobspec Nomad вместе со своим планировщиком — один такой; воркер М9 — другой. |
| ~~**worker**~~ | *Устарел (14 сентября 2026).* Имя воркер-процесса в М9 — класс `Worker` в `recorder/worker/`: один процесс, цикл сверки и до пятидесяти конвейеров. Переименован, чтобы слово М9 и слово М10 (`vmsworker`) были одним словом для одной вещи. |
| **Ревизия** (*Revision*) | Монотонное целое, присваиваемое контроллером, своё на каждый объект. *Применено* значит `observed_revision >= revision`. |
| **Эпоха** (*Epoch*) | Токен отсечения: растёт при каждой смене владельца, никогда не убывает, выдаётся ровно одним авторитетом. Часть пути в архиве — поэтому просроченный писатель не может назвать те файлы, которые иначе испортил бы. |
| **Отсечение** (*Fencing*) | Сделать записи просроченного писателя безвредными вместо того, чтобы пытаться их остановить. Зомби от записи не остановить. |
| **CAS** — check-and-set | Обновление, которое проходит только если значение не изменилось с момента, когда вы его прочитали. Переменные Nomad сравнивают с `ModifyIndex` и возвращают 409 на конфликте; именно так курс выдаёт эпохи. **Блокировка** без токена отсечения — не замена. |
| **Split-brain** | Два писателя, каждый из которых считает, что владеет одним и тем же объектом. Для key-value-хранилища переживаемо. Для видео — порча: у видео нет функции слияния. |
| **Кворум** (*Quorum*) | Большинство, которое нужно группе консенсуса, чтобы договориться. Отсюда нечётное число серверов и отсюда же узел-свидетель. |

---

## Доступность и восстановление

| | |
|---|---|
| **HA** — высокая доступность (*high availability*) | Держать сервис работающим через отказ одной из его частей, обычно с готовым к подхвату резервом. В М11 вопрос в том, нужна ли она *каталогу*. |
| **Переезд при отказе** (*Failover*) | Перенос работы с того, что отказало. В этом курсе он происходит на двух уровнях: планировщик переносит **воркера** на здоровый сервер; домен переназначает **камеры** только тогда, когда оператор перебалансирует. |
| **RPO** — целевая точка восстановления (*recovery point objective*) | Сколько недавно записанных данных авария может потерять, в единицах времени. Каждая граница в этом курсе пересекается одинаково — **односторонней публикацией с объявленным RPO**, — пересекает её видео (spool из М9), конфигурация (М11) или статус. Число, которое продукт объявляет, а не неожиданность, которую он обнаруживает. |
| **RTO** — целевое время восстановления (*recovery time objective*) | Сколько времени разрешено занимать восстановлению. В сдаче М11 это секунды между выдернутым питанием и возобновившейся записью. |
| **TTL** — время жизни (*time to live*) | Сколько аренда действительна, прежде чем истечёт и объект можно будет выдать кому-то другому. |
| **Узел-свидетель / монитор** (*Witness / monitor node*) | Член кластера базы данных, не хранящий данных и присутствующий только чтобы разбивать ничьи и не допускать split-brain. |

---

## Хранение и процесс

| | |
|---|---|
| **Spool** | Сегменты, записанные локально и ждущие выгрузки, удаляемые только по подтверждению. Появился в М9, чтобы обрыв канала стоил видимости, а не видео; М9 кладёт над теми же файлами индекс, и они становятся архивом. |
| **RSS** — размер резидентного набора (*resident set size*) | Память, которую процесс держит в RAM. Страницу разделяемой библиотеки считает один раз **на процесс**, поэтому суммирование по процессам считает её дважды и завышает ответ. |
| **PSS** — пропорциональный размер набора (*proportional set size*) | То же самое, но каждая разделяемая страница делится на число процессов, которые её отображают. **Единственный честный способ** сравнить один процесс с N конвейерами против N процессов с одним конвейером каждый. |
| **GIL** — глобальная блокировка интерпретатора (*global interpreter lock*) | Блокировка CPython, гарантирующая, что байткод Python в один момент исполняет один поток. Отпускается вокруг вызовов в C — поэтому пятьдесят конвейеров GStreamer в одном процессе Python работают, — и берётся заново в каждом колбэке, поэтому колбэк на каждый буфер не работает. |
| **CSI** — Container Storage Interface | Стандарт, по которому планировщики подключают сетевое хранилище к нагрузкам. Nomad его поддерживает; М11, урок 3 объясняет, почему это не даёт прибору переезд без участия человека. |
| **SAN / NAS** | Storage area network / network-attached storage — разделяемое хранилище, доступное нескольким серверам. Дорого и общий домен отказа. |

---

## Безопасность и личность

| | |
|---|---|
| **Аутентификация** (*Authentication*) | Доказательство того, *кто* субъект. Федерирована в М12, поэтому Алиса одна на все кластеры и домены. |
| **Авторизация** (*Authorization*) | Решение о том, что этому субъекту *можно*. Намеренно **не** централизована: гранты живут в каждом кластере, чтобы их можно было проверить при недоступном домене, и несут срок, который ограничивает окно отзыва. |
| **PKI** — инфраструктура открытых ключей (*public key infrastructure*) | Удостоверяющие центры, сертификаты и механика отзыва, которые позволяют сторонам доказать, кто они. |
| **CA** — удостоверяющий центр (*certificate authority*) | То, что подписывает сертификаты. Каждый домен держит свой — *подписывающий домена* — и **его корень самоподписан и принадлежит заказчику, навсегда.** Никакого вендорского корня над ним, потому что вендор, который может подписать ваш CA, может выдать себя за ваш домен. Восстанавливаемость — резервной копией и ротацией, а не делегированием. |
| **Корень доверия** (*Trust anchor*) | Сертификат, который сторона решила считать доверенным без дальнейших доказательств, — вершина любой цепочки, которую она примет. В этом дизайне он ровно один на домен, это собственный корень домена, и над ним ничего нет. |
| **Кросс-подписание** (*Cross-signing*) | Выдача второго сертификата для того же ключа другим CA, чтобы цепочка проверялась и под старым, и под новым корнем сразу. Так домен ротирует собственный корень без дня переключения — М12, урок 7 превращает это в учение. |
| **mTLS** — взаимный TLS (*mutual TLS*) | Оба конца соединения предъявляют сертификаты, так что сервер аутентифицирует клиента так же, как клиент сервер. |
| **CRL / OCSP** | Certificate revocation list / Online Certificate Status Protocol — два способа спросить, отозван ли сертификат. Оба предполагают, что до чего-то можно дотянуться, — поэтому на устройстве М12 предпочитает короткие сроки жизни. |
| **TPM** — Trusted Platform Module | Чип, который держит неизвлекаемый ключ и умеет засвидетельствовать, какой софт загрузился. Самый сильный ответ на вопрос *как коробка без секрета получает секрет*. |
| **BRSKI** | Bootstrapping Remote Secure Key Infrastructure ([RFC 8995](https://datatracker.ietf.org/doc/html/rfc8995)) — стандарт защищённого ввода в строй без касаний. |
| **IDevID / LDevID** | Initial / Local Device Identifier — сертификат, который производитель ставит на фабрике, и тот, который домен выдаёт после того, как устройство вошло. |
| **MASA** | Manufacturer Authorized Signing Authority — служба на стороне вендора, которая ручается за устройство перед доменом. Держать такую — обязательство навсегда, и М12 говорит это прямо. |
| **EST** | Enrollment over Secure Transport — как устройство просит у CA свой сертификат после того, как решило, кому доверять. |

---

## Лицензирование

| | |
|---|---|
| **BUSL** — Business Source License | Source-available лицензия: исходники публикуются, промышленное использование разрешено при условиях, и каждая версия переходит в open-source-лицензию после **Change Date**. **Nomad — единственный BUSL-компонент в дизайне** — Consul и Vault из него убрали при проектировании — и лицензиар у него IBM. Для этого продукта разрешён по тесту на конкуренцию; см. [`COURSE-PLAN.md`](./COURSE-PLAN.md). |
| **Change Date** | Когда грант BUSL для конкретной версии переходит — через четыре года после публикации этой версии, в MPL 2.0 (Nomad 1.8.0 → 28 мая 2028). Это не путь к отступлению: версия доходит до своей Change Date примерно через два года *после* того, как её поддержка кончилась, так что сборка под MPL всегда та, которую уже не патчат. |
| **Additional Use Grant** | Пункт, определяющий, какое промышленное использование разрешено. У Nomad он запрещает предлагать софт третьим сторонам *хостингом или встраиванием* **в конкуренции с платными продуктами лицензиара** — а «встраивание» само определено относительно *конкурирующего продукта*, так что одно встраивание триггером не является. VMS не пересекается с Nomad Enterprise, поэтому поставлять прибор с Nomad внутри разрешено; см. [`COURSE-PLAN.md`](./COURSE-PLAN.md). |
| **MPL 2.0** | Mozilla Public License 2.0 — open-source-лицензия со слабым копилефтом; то, во что переходят BUSL-компоненты. |

*Это изложение условий лицензий, а не юридическая консультация.*
