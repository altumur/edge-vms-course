# Модуль 10A — Платформа: то, что ничего не знает о видео

[Модуль 9](../М9_EdgeVMS/README.md) заканчивается коробкой, которая владеет своей ОС и своей правдой: A/B-корень под RAUC и Node, чей Postgres хранит то, что *должно быть*, пока воркер делает это правдой. Модуль 10 разбирает этот Node и собирает заново на форме, к которой курс пришёл, — **контроллер, воркеры и ресурсы**. Этот модуль — первая половина сборки: **платформа, и только она**.

Граница между двумя половинами проведена не по вкусу, а там, где её уже проверяет тест: в `w2cplatform/` нет ни одного импорта из `vms/` и нет даже слова *camera*. Всё, что вы напишете здесь, не знает, что такое камера, — и именно поэтому [М10B](../М10B_ServerVMS/README.md) сможет положить на это четыре подсистемы, не тронув ни строки платформы.

Двадцать уроков, и каждый пишет один файл или половину большого — функцию за функцией, в том порядке, в каком их пришлось бы писать с нуля. Ни один урок не заканчивается кодом, который не запускается: к концу каждого прогон тестов зелёный.

> **Где лежит код.** Пакет один на оба модуля: [`../vmsserver/`](../vmsserver/README.md). В этом модуле вы пишете его часть `w2cplatform/` и её тесты; в М10B — `vms/`, `gstvms/` и `deploy/`. Код в пакете — на английском (имена, комментарии): это тот же пакет, с которого снят Go-порт и на котором стоит М11. Уроки — на русском; фрагменты кода цитируются как есть.

## Тезис

| | Что даёт платформа | Что даёт подсистема |
|---|---|---|
| Хранение | хранилище конфигурации с `ModifyIndex` и CAS; хранилище объектов; один писатель на префикс | свой префикс `<имя>/*` и одну YAML-строку о том, что в нём лежит |
| Личность | слот, взятый по CAS: `N` процессов получают устойчивые имена, не спрашивая никого | число `N` не решает никто изнутри — его решает планировщик |
| Отсечение | эпоха на единицу работы (fencing token) и аренда на монотонных часах | ключ, в который она кладёт эпоху |
| Размещение | `SpecController`, прочитанный из YAML: capacity, метки, причина, политики | одну YAML с полями, ограничением и снимком |
| Экран | `SpecConsole` и одна страница, собранная из `/spec` | ничего: страница строит себя сама |
| События | бакеты на ресурсе, база-кэш над ними, слияние без базы у консоли | воркера, который держит эпоху и пишет строку |

> **Контроллер пишет, платформа хранит, воркер читает свою долю.** Платформа знает форму подсистемы и ничего — о камере. Тест `test_the_platform_knows_nothing_about_video` ищет в `w2cplatform/` импорт из `vms/` и слово *camera*; он должен остаться зелёным после каждого урока этого модуля.

## Уроки

| # | Урок | Вы сможете... |
|---|---|---|
| 1 | [Что такое платформа, и чем она не является](01-what-a-platform-is.md) | Назвать три роли и записать контракт подсистемы таблицей; развернуть пакет и его прогонщик тестов без pytest; собрать «коробку» в `/tmp`, на которой работает вся сюита; поставить два теста чистоты, которые держат границу весь модуль. |
| 2 | [Хранилище конфигурации](02-the-config-store.md) | Написать `FileVariables` целиком — `get`, `put` с CAS, `delete`, `list`; объяснить, почему запись через `rename` атомарна, а `ModifyIndex` не начинается с нуля; доказать четырьмя потоками, что CAS выигрывает ровно один. |
| 3 | [Один писатель на префикс](03-one-writer-per-prefix.md) | Превратить «так принято» в `Forbidden`: `as_writer`, ACL как словарь префиксов, проверка до взятия блокировки; выписать заранее все токены системы и сказать, почему консоль, умеющая размещать, — это второй контроллер с браузером. |
| 4 | [Хранилище объектов и heartbeat](04-the-object-store.md) | Сказать, чем объект отличается от строки конфигурации, и написать `FsObjectStore`; получить «целиком или никак» из одного `os.replace`; задать форму heartbeat, которую будут публиковать все воркеры курса. |
| 5 | [Раскладка ключей](05-the-key-layout.md) | Написать `Subsystem` — `config`, `assignment`, `heartbeat_key`, `epoch_key`, `slot_key` и два ACL — и увидеть все ключи системы на одной странице до того, как появится первая подсистема. |
| 6 | [Отсечение: эпоха и аренда](06-fencing-the-epoch-and-the-lease.md) | Написать `next_epoch` с его циклом повторов по CAS, `current_epoch` и `Lease` на поддельных часах; сказать, почему аренда решает локально, а эпоха — глобально, и какой из двух слоёв ловит зомби первым. |
| 7 | [Личность через захват](07-identity-by-claim.md) | Написать `Slot`, `claim_slot`, `renew_slot`, `release_slot`; показать, как замена наследует просроченный слот вместе с назначением, и почему индекс планировщика — это предпочтение, а Variable — доказательство. |
| 8 | [Две базы](08-the-two-bases.md) | Написать `Controller` с его `write` (прочитать, изменить, CAS, повторить) — образцом каждой последующей записи — и `Worker` с воротами: `take_epoch`, `may_write`, `renew_leases`, `heartbeat`. |
| 9 | [Подсистема — это YAML](09-a-subsystem-is-a-yaml.md) | Прочитать десять строк YAML в `SubsystemSpec`: `Field` с типами и значениями по умолчанию, `Derived`, `row`/`items`, `parse_id`. |
| 10 | [Что токену позволено писать](10-what-a-token-may-write.md) | Написать `refuse`, `new_row`, `PLATFORM_FIELDS` и два ACL, вырезанных из одной спецификации; объяснить, почему поля `worker`, `epoch`, `revision` оператор не отправляет никогда. |
| 11 | [`SpecController` I — строки](11-speccontroller-the-rows.md) | CRUD по CAS: `create`, `update`, `delete`, счётчик идентификаторов, производные строки, `revision`, идемпотентное создание. |
| 12 | [`SpecController` II — размещение](12-speccontroller-placement.md) | Собрать причину размещения по частям: `eligible`, `_pool`, `_best`, `_pick`, `holder_near`, `home_for`, `place`, `ensure_placed`, `ensure_home`, `move`, `unplaceable`. |
| 13 | [`SpecController` III — молчания, ручки, снимок](13-speccontroller-the-silences.md) | `resource_state`, `without_resource`, `idle_by_policy`, `gone_servers`, `redistribute`, `rebalance`; политики `servers: shared \| distinct`; `read_model`, `snapshot`, `failover_seconds`. |
| 14 | [События: бакеты на ресурсе](14-events-buckets-on-a-resource.md) | Написать `events.py` целиком: путь бакета, `EventLog.append`, `read_bucket`, `buckets_under`, `subsystems_under` — форму данных, у которой один писатель по построению. |
| 15 | [База, которая является кэшем](15-the-database-that-is-a-cache.md) | `EventDatabase` над собственным деревом ресурса — `rebuild`, `tail`, `query`, `forget` — и `MergedIndex`: то, что есть у консоли вместо базы. |
| 16 | [Ресурс как работа платформы](16-the-resource-as-a-platform-job.md) | `Resource`: регистрация чужих проходов, heartbeat, хранение по строке каждой подсистемы, зеркало к соседу и возврат домой после замены диска; `serve` с его маршрутами. |
| 17 | [Консоль как данные](17-the-console-as-data.md) | `SpecConsole`: `describe`, `dispatch` и чтения — `/spec`, `/<rows>`, `/where`, `/unplaceable`, `/events`; идемпотентный ключ, который живёт в хранилище, а не в памяти процесса. |
| 18 | [Один процесс, несколько подсистем](18-one-process-several-subsystems.md) | Записи, `/metrics`, `/servers`, `/policy`, `/marks` и `Mount` — корень на `/` и остальные под своими именами. |
| 19 | [Страница](19-the-page.md) | 666 строк `console.html`: список и формы, собранные из `/spec`; таймлайн, метки событий, плеер с диапазонными запросами, переключатели оператора и администратора. |
| 20 | [Оркестратор выбирается при установке](20-the-orchestrator-is-an-install-time-choice.md) | Три места, где вендор просачивается в цикл, и шов в каждом: `open_vars` со схемами URL, пять нейтральных имён `runtime.py`, непрозрачный индекс — и контракт из шести пунктов, гоняемый против любого хранилища через `CONTRACT_URL`. |
| 21 | [Когда место кончается](21-when-the-disk-fills.md) | Отличать обещание (ретенция по дням) от предохранителя: `disk_space` через `statvfs`, две отметки и зазор в часах записи, `relieve` и дверь `hook.free` — платформа считает байты, подсистема решает, чем; пол, недостача вслух и эвакуация без кнопки. |
| 22 | [Обновление без простоя](22-rolling-upgrade.md) | Плановая остановка — не молчание: `platform/drain` с одним именем, третий член в `_pool` и третья причина в `redistribute`; сухой прогон `would_strand`; готовность как условие (`safe`), а не таймаут; и вторая половина — `SCHEMA`, отказ старой сборки и номер, который поднимают последним. |
| 23 | [Секрет в строке](23-a-secret-in-a-row.md) | У секрета две законные точки касания: оператор пишет, воркер читает. Правило — суффикс `_secret`, объявленный там же, где поле; маскирование — копия и три вызова, включая тот, чей ответ сохраняется под ключом идемпотентности; снимок отказывает при загрузке, а по умолчанию фильтрует; тип `url` закрывает дверь, через которую пароль заезжает в поле, о котором маска не думает. И то, чего это не даёт, записано рядом. |
| 24 | [Одна платформа, две операционные системы](24-one-platform-two-operating-systems.md) | Две функции — `flock` и `statvfs` — решали, поедет ли платформа на Windows. Форма отказа разная и определяет тест: в Python не импортировался модуль, поэтому тест импортирует; в Go не собирался пакет, поэтому тест собирает под три ОС. Шов один на каждую функцию — `_lock_exclusive` и файл с тегом сборки, — вызов Windows без единой зависимости и про то, когда так делать нельзя; и граница между «собирается» и «работает», записанная вслух. |
| 25 | [Один объект на кластер или один на воркера](25-one-object-per-worker.md) | Снимок был единственным объектом платформы, растущим вглубь, а не вширь: 232 КиБ на проектных 600 камерах под потолком Nomad Variables в 64 KiB — и падал он тихо, под строкой лога «placement pass failed». Нарезка по воркеру — форма, которая уже была у heartbeat'а в соседнем файле; зарезервированное имя `unplaced` с правилом, которое его резервирует; пустой объект как способ сказать «здесь больше ничего нет» в хранилище без удаления; и читатель, у которого возраст целого — возраст самого старого шарда, а камера на переезде не становится аварией. Почему «сменить хранилище на S3» — это снять будильник, а не вылечить. |

## Демо, от которого модуль построен назад

Коробка, на которой нет ни одной камеры. `python3 -m vms console` не запускается — в этом модуле его ещё нет. Вместо него — двадцать строк в уроке 13:

```python
spec = SubsystemSpec.load("counter.subsystem.yaml")   # rows: ticks, два поля, capacity в тиках
ctl  = SpecController(spec, vars_, objects)
ctl.create({"name": "первый", "every": 5})            # оператор создал единицу работы
ctl.ensure_placed()                                   # контроллер разместил её на воркере, который бьётся
```

Подсистема `counter` не существует как код: это пятнадцать строк YAML из урока 9 и сорок строк воркера в тексте урока 13. Она размещается, переживает падение своего воркера, показывает себя на странице и пишет события на ресурс — **через тот же `SpecController`, ту же `SpecConsole` и тот же `Resource`**, которые в М10B будут возить видео. Это и есть доказательство, ради которого модуль отделён: платформа не знает, что такое камера, и не узнает.

## Что можно проверить без железа

Всё. Двадцать восемь тестов `w2cplatform/` не требуют ни GStreamer, ни сети: персистентность и CAS с четырьмя гонящимися потоками; выдача эпох, ни одна не повторена; аренда на поддельных часах; ACL; слоты — захваченные, просроченные, унаследованные, освобождённые; ресурс, зеркалящий бакеты подсистем, которых не существует, и возвращающий их себе после замены диска; база событий, удалённая и пересобранная к тому же ответу; консоль на настоящем порту с идемпотентным ретраем; и два теста чистоты, которые не дают платформе узнать про видео.
